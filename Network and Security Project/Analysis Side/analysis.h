/*
 Network and Security Project - Analysis Side.
 Header File
 */

#include "../Global/general.h"


//The side to initialize the code from another table to make sure it can be use in the main.
extern int decode_table_check();
extern int account_register_toBank();
extern int app_register_toDirector();
extern int address_initialization();
extern int eCent_use(char *eCent);
extern int network_module();
